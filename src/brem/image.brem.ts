
export class BremImage {
    filePath: string;
    aspectRatio: number;
    backdrop: boolean;
    poster: boolean;
    height: number;
    width: number;
    languageCode: string;
    averageVote: number;
    voteCount: number;

    //no constructor because of the number of fields. use default constructor
}